import React from 'react'

function TypeRadioForm({state, setState}) {
    const {
    title,
    url,
    categories,
    loadedCategories,
    buttonText,
    success,
    error,
    type,
    medium,
  } = state;

  return (
    <div>

    </div>
  )
}

export default TypeRadioForm
