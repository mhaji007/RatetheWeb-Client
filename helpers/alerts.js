export const showSuccessMessage = (success) => (
  <div className="alert alert-success mt-0">{success}</div>
);
export const showErrorMessage = (error) => (
  <div className="alert alert-danger mt-0">{error}</div>
);
